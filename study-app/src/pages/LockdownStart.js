import React, { Component } from 'react';

class LockdownStart extends Component {
    render() {
        return (
            <div>lockdown start</div>
        );
    }
}

export default LockdownStart;