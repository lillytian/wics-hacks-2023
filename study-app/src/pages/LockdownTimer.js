import React, { Component } from 'react';

class LockdownTimer extends Component {
    render() {
        return (
            <div>lockdown timer</div>
        );
    }
}

export default LockdownTimer;